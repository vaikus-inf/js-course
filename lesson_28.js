//Методы и свойства строк и чисел (Методы - вспомогательные функции. Свойства - вспомогательные значения.)

"use strict";

console.dir(Number); //Если это запустить в консоли браузера, можно увидеть свойства и методы объекта Number

const str = 'Test';

console.log(str.length); //Используем свойство (property) length
console.log(str.toUpperCase()); //Используем метод toUpperCase
console.log(str.toLowerCase());
